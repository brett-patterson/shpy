from setuptools import setup, find_packages

setup(
    name='shpy',
    version='0.1',
    description='Simplified subprocess execution for Python',
    author='Brett Patterson',
    packages=find_packages()
)
